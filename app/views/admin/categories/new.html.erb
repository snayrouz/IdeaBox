<div class="container-fluid">
  <nav class="navbar navbar-expand navbar-dark bg-dark" role="navigation">
  <div class="container-fluid">
    <a href="/" class = "navbar-brand">IDEABOX</a>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user.role == "admin" %>
        <li><%= link_to "New Category", new_admin_category_path %></li>
        <li><%= link_to "Upload Images", new_admin_image_path %></li>
        <% end %>
        <% if logged_in? %>
          <li><%= link_to "Logout", logout_path, method: :delete %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
<div class="jumbotron">
  <h1>New Category:</h1>
  <%= render partial: 'form' %>
  </div>
  <footer>
    <%= link_to 'Back to All Categories', admin_categories_path %>
  </footer>
</div>
